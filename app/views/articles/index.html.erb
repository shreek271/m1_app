<p id="notice"><%= notice %></p>
<h1>Articles</h1>
<%= content_tag(:p,"No Articles found",class: :strong) if @articles.empty? %>
<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article %></li>
    </tr>
  <% end %>
</ul>

<br>

